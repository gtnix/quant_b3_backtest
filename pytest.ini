[pytest]
minversion = 7.0
testpaths = tests
pythonpath = .
addopts = -q
markers =
    core: core logic tests (score computation, pure functions)
    signal: signal generation validation without execution
    mechanical: order handling and execution mechanics
    smoke: minimal smoke tests for quick validation
    data_quality: data preparation and alignment validations

