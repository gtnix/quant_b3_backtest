# Strategy Factory Campaign Configuration - SMOKE TEST
# Generated for: scg_smoke_001
# Purpose: Minimal smoke test to validate pipeline end-to-end

[campaign]
name = "scg_smoke_001"
tag = "smoke"
owner = "debug_agent"
notes = "Smoke test para validar pipeline end-to-end"

[dataset]
market = "BR"
start_date = "2022-01-01"
end_date = "2024-06-01"
universe = "ibov"
# data_path = "data/ohlcv"  # Optional: for dataset hash

[evolution]
# base_config = "configs/optimization/scg_base.toml"
population_size = 32        # Pequeno para smoke
max_generations = 5         # Pouquissimas geracoes

[execution]
config_path = "configs/execution_institutional.toml"
delay_bars = 1

[seeds]
count = 1                   # Apenas 1 seed
base_seed = 42

[budget]
max_runs = 1
top_k = 5                   # Top 5 candidatos
timeout_per_run_secs = 180  # 3 minutos max
stress_enabled = false      # Desabilitar stress no smoke

[promotion]
min_oos_sharpe_net = 0.0    # Permissivo para teste
max_pbo = 1.0               # Permissivo
min_stress_passed = 0       # Permissivo
gates_required = false      # Permissivo

[data_integrity]
mode = "fast"
max_gap_days = 10
jump_threshold_pct = 30.0
price_adjustment = "adjusted"
universe_type = "static"    # Acknowledge static universe
enabled = true
